import React from 'react';

export class FormMultipleLanguage extends React.Component {
    state = {  };

    componentDidMount() {
        $(document).ready(() => {
            // const { tabRender = false, languages = ['vi', 'en'], title, gridClassName = 'col-md-12', className = '', formElement, readOnly, required } = this.props;
            // languages.forEach(code => {
            //
            // });

        });
    }

    render() {
        // const {} = this.props;
        return <div>

        </div>;
    }
}